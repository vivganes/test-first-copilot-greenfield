# AddressRepository

**Properties:**
- db_session: DatabaseSessionManager

**Methods:**
- add_address(address: Address): Adds a new address
- get_address_by_user(user_id: int): Retrieves address for user

**Related Classes:**
- Address: Entity managed
- DatabaseSessionManager: Manages DB connection
