# AddressInputComponent

## Properties
- `addressForm: FormGroup` — Form group for address fields.
- `address: Address` — Model representing the address.

## Methods
- `buildAddressForm()` — Initializes address form group and validators.
- `getAddressValue(): Address` — Returns the current address value.
- `validateAddress()` — Validates address fields.

## Related Classes
- `RegistrationFormComponent`: Uses this component for address input.
- `Address`: Data model for address details.
